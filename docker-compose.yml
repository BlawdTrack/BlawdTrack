services:
  blawdtrack-app:
    build: .
    container_name: blawdtrack_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true